# Local-test-only dependencies — NOT installed by the production
# Cloud Run / Cloud Build images. Production gets pydantic-ai etc. from
# requirements.txt; the only local extra is the Podman sandbox backend
# used by the manual-run harness (cli/model_test.py), because Cloud Run
# can't nest containers.
#
# Local install:
#   pip install -r services/page_builder/requirements.txt \
#               -r services/page_builder/requirements-test.txt

llm-sandbox[podman]>=0.3.44     # rootless Podman sandbox; needs `apt install podman`
