# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
ENV/
env/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment files
.env
.env.local
.env.*.local

# Data (downloaded datasets)
data/downloads/
data/*.db

# Keep the data directory structure
!data/.gitkeep
!data/downloads/.gitkeep

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Test
.pytest_cache/
.coverage
htmlcov/

# Frontend (Nuxt 3)
frontend/node_modules/
frontend/.output/
frontend/.nuxt/
frontend/.data/
# Curated viz libs copied from node_modules at build time + the
# hand-written gov-explorer.js copied from frontend/scripts/. See
# frontend/scripts/copy-libs.mjs.
frontend/public/lib/
# Publisher-generated artifacts. Materialized at build time from Firestore
# (data.json, agent_data.json, manifest.json, search-index.json) and rsynced
# from GCS staging (content.html). Single source of truth lives upstream —
# git stays code-only.
frontend/public/datasets/
frontend/public/data/manifest.json
frontend/public/data/search-index.json
# Regenerated by scripts/build-sitemap.mjs on every `npm run generate`; the
# deployed copy is the one it writes into .output/public/.
frontend/public/sitemap.xml
# Deployment-specific, generated by scripts/copy-libs.mjs when
# NUXT_PUBLIC_ADSENSE_ID is set.
frontend/public/ads.txt

# Infra secrets
infra/pages.build-hook.txt
infra/service-account*.json


# Playwright MCP artifacts
.playwright-mcp/
home.png
antennas.png
deadsea.png

# model_test harness scratch dir (tmp/model_test/<id>/...)
tmp/

# Model-eval comparison snapshots — kept locally, not in the repo (agent
# output embeds raw dataset content, incl. personal contact details from
# registry datasets). Methodology: docs/MODEL_EVAL.md.
services/page_builder/harness-comparison/

# Code-graph tool output (local analysis artifact, not part of the project)
graphify-out/

# Nuxt build output + stray dirs
frontend/dist
frontend/3000/

# Local Claude Code settings
.claude/

# Firebase local artifacts
firebase-debug.log
firestore-debug.log
.firebase/
